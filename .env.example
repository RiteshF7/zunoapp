# Zuno — single source of truth for dev and prod
# Copy to .env and fill values. You only edit this file.
#
# Local: switch active environment (writes backend/.env and ui/.env)
#   ./scripts/use-dev.sh   → dev (rvp Supabase, localhost API)
#   ./scripts/use-prod.sh  → prod (izx Supabase, render API)
#
# Deployed (e.g. Render): set env vars in the host dashboard (ENVIRONMENT=production,
# SUPABASE_URL, SUPABASE_SERVICE_ROLE_KEY, etc.). No .env file on the server.
# ZUNO_MODE is used when you run ./scripts/resolve-env.sh with no --mode.

ZUNO_MODE=development

# ═══════════════════════════════════════════════════════════════════════════
# Supabase — Dev (separate dev Supabase project)
# ═══════════════════════════════════════════════════════════════════════════
SUPABASE_URL_DEV=https://your-dev-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY_DEV=
SUPABASE_JWT_SECRET_DEV=
SUPABASE_DB_PASSWORD_DEV=

# ═══════════════════════════════════════════════════════════════════════════
# Supabase — Prod (separate prod Supabase project)
# ═══════════════════════════════════════════════════════════════════════════
SUPABASE_URL_PROD=https://your-prod-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY_PROD=
SUPABASE_JWT_SECRET_PROD=
SUPABASE_DB_PASSWORD_PROD=

# ═══════════════════════════════════════════════════════════════════════════
# UI (Vite) — Dev (used by Android debug build)
# ═══════════════════════════════════════════════════════════════════════════
VITE_SUPABASE_URL_DEV=https://your-dev-project.supabase.co
VITE_SUPABASE_ANON_KEY_DEV=
VITE_API_BASE_DEV=http://localhost:8000

# ═══════════════════════════════════════════════════════════════════════════
# UI (Vite) — Prod (used by Android release build)
# ═══════════════════════════════════════════════════════════════════════════
VITE_SUPABASE_URL_PROD=https://your-prod-project.supabase.co
VITE_SUPABASE_ANON_KEY_PROD=
VITE_API_BASE_PROD=https://zunoapp.onrender.com

# ═══════════════════════════════════════════════════════════════════════════
# Chrome extension & default app/API URLs (injected by resolve-env)
# ═══════════════════════════════════════════════════════════════════════════
ZUNO_APP_URL_DEV=http://localhost:5173/app/
ZUNO_APP_URL_PROD=https://zunoapp.onrender.com/app/
ZUNO_API_BASE_DEV=http://localhost:8000
ZUNO_API_BASE_PROD=https://zunoapp.onrender.com
# VITE_SHOW_FEED_PROD=true   # Set to show Feed nav and #feed route (omit or false = Home shows Library Saved only)

# ═══════════════════════════════════════════════════════════════════════════
# Vertex AI — Dev (optional for local dev)
# ═══════════════════════════════════════════════════════════════════════════
GCP_PROJECT_ID_DEV=
GCP_CREDENTIALS_JSON_DEV=

# ═══════════════════════════════════════════════════════════════════════════
# Vertex AI — Prod
# ═══════════════════════════════════════════════════════════════════════════
GCP_PROJECT_ID_PROD=
GCP_CREDENTIALS_JSON_PROD=

# ═══════════════════════════════════════════════════════════════════════════
# Backend — Dev (ENVIRONMENT and CORS_ORIGINS)
# ═══════════════════════════════════════════════════════════════════════════
ENVIRONMENT_DEV=development
CORS_ORIGINS_DEV=http://localhost:5173,http://localhost:8081,http://localhost:19006,http://localhost:8000

# ═══════════════════════════════════════════════════════════════════════════
# Backend — Prod
# ═══════════════════════════════════════════════════════════════════════════
ENVIRONMENT_PROD=production
# Include http://localhost and capacitor://localhost so Android/iOS native app (Capacitor WebView) can call the API.
CORS_ORIGINS_PROD=https://zunoapp.onrender.com,https://www.zuno.com,https://zuno.com,http://localhost,capacitor://localhost

# ═══════════════════════════════════════════════════════════════════════════
# Shared (same for both dev and prod)
# ═══════════════════════════════════════════════════════════════════════════
GCP_LOCATION=us-central1
RAG_CHUNK_SIZE=500
RAG_CHUNK_OVERLAP=50
RAG_TOP_K=8
VERTEX_EMBEDDING_MODEL=text-embedding-005
VERTEX_LLM_MODEL=gemini-2.0-flash-001
BACKEND_PORT=8000
