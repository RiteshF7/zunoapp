# ============================================================================
# ZUNO APP — Environment Variables
# ============================================================================
# Copy this file to .env and fill in your actual values.
#   cp .env.example .env
#
# NEVER commit the .env file to git!
# ============================================================================


# ────────────────────────────────────────────────────────────────────────────
# SUPABASE (Required)
# ────────────────────────────────────────────────────────────────────────────
# Where to find: Supabase Dashboard → Settings → API
# The EXPO_PUBLIC_ prefix makes these available at build time in Expo.

EXPO_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...your-anon-key-here


# ────────────────────────────────────────────────────────────────────────────
# SUPABASE PROJECT REF (for CLI linking)
# ────────────────────────────────────────────────────────────────────────────
# Where to find: The subdomain in your Supabase URL above
#   e.g. if URL is https://abcdefghijkl.supabase.co → ref is "abcdefghijkl"
# Usage: npx supabase link --project-ref $SUPABASE_PROJECT_REF

SUPABASE_PROJECT_REF=<your-project-ref>


# ────────────────────────────────────────────────────────────────────────────
# OPENAI (Required for AI features — Edge Functions)
# ────────────────────────────────────────────────────────────────────────────
# Where to find: https://platform.openai.com/api-keys
# This is NOT used directly in the app. It is set as a Supabase remote secret:
#   npx supabase secrets set OPENAI_API_KEY=sk-...
# Also used by Supabase Studio AI assistant if running locally.

OPENAI_API_KEY=sk-...your-openai-api-key-here


# ────────────────────────────────────────────────────────────────────────────
# EAS BUILD (Required for Expo EAS builds)
# ────────────────────────────────────────────────────────────────────────────
# Where to find: Generated by running `eas init` after `eas login`
# Goes into: app.json → expo.extra.eas.projectId

EAS_PROJECT_ID=your-eas-project-id


# ════════════════════════════════════════════════════════════════════════════
# OPTIONAL — Only needed if you enable these features
# ════════════════════════════════════════════════════════════════════════════


# ────────────────────────────────────────────────────────────────────────────
# OAUTH PROVIDERS (Optional — configured in Supabase Dashboard)
# ────────────────────────────────────────────────────────────────────────────
# Apple Sign-In: https://developer.apple.com → Certificates, IDs & Profiles
# Google Sign-In: https://console.cloud.google.com → Credentials
#
# These are set in Supabase Dashboard → Authentication → Providers,
# NOT in this .env file. Keeping them here just as a reference/checklist.

# SUPABASE_AUTH_EXTERNAL_APPLE_CLIENT_ID=com.zuno.app
# SUPABASE_AUTH_EXTERNAL_APPLE_SECRET=your-apple-secret

# SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
# SUPABASE_AUTH_EXTERNAL_GOOGLE_SECRET=your-google-client-secret


# ────────────────────────────────────────────────────────────────────────────
# SMTP (Optional — for production email delivery)
# ────────────────────────────────────────────────────────────────────────────
# Where to find: Your email provider (SendGrid, Resend, Mailgun, etc.)
# Configure in: Supabase Dashboard → Settings → Auth → SMTP Settings
#   OR in supabase/config.toml under [auth.email.smtp] for local dev

# SMTP_HOST=smtp.sendgrid.net
# SMTP_PORT=587
# SMTP_USER=apikey
# SMTP_PASS=SG.your-sendgrid-api-key
# SMTP_ADMIN_EMAIL=noreply@yourdomain.com
# SMTP_SENDER_NAME=Zuno


# ────────────────────────────────────────────────────────────────────────────
# TWILIO SMS (Optional — for SMS/Phone auth)
# ────────────────────────────────────────────────────────────────────────────
# Where to find: https://console.twilio.com
# Configure in: Supabase Dashboard → Authentication → Phone Provider

# SUPABASE_AUTH_SMS_TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# SUPABASE_AUTH_SMS_TWILIO_AUTH_TOKEN=your-twilio-auth-token
# SUPABASE_AUTH_SMS_TWILIO_MESSAGE_SERVICE_SID=MGxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx


# ============================================================================
# SETUP CHEAT SHEET
# ============================================================================
#
# 1. Fill in the REQUIRED values above
#
# 2. Link Supabase project:
#      npx supabase login
#      npx supabase link --project-ref <your-project-ref>
#
# 3. Push database migrations:
#      npx supabase db push
#
# 4. Set Edge Function secrets (on remote):
#      npx supabase secrets set OPENAI_API_KEY=sk-...
#
# 5. Deploy Edge Functions:
#      npx supabase functions deploy process-content --no-verify-jwt
#      npx supabase functions deploy generate-embedding --no-verify-jwt
#      npx supabase functions deploy generate-feed --no-verify-jwt
#
# 6. (Optional) Setup EAS:
#      npm install -g eas-cli
#      eas login
#      eas init
#      → Copy the generated projectId into app.json
#      → Update EXPO_PUBLIC_SUPABASE_ANON_KEY in eas.json for each profile
#
# 7. Start developing:
#      npm install
#      npm start
#
# ============================================================================
