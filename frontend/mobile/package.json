{
  "name": "zuno-mobile",
  "version": "1.0.0",
  "description": "Zuno mobile app — Capacitor wrapper for the HTML UI",
  "private": true,
  "scripts": {
    "sync": "npx cap sync",
    "open:android": "npx cap open android",
    "build:android": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "open:ios": "npx cap open ios",
    "build:ios": "npx cap sync ios",
    "copy": "npx cap copy"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/browser": "^8.0.0",
    "@capacitor/core": "^8.0.2",
    "@capacitor/ios": "^8.1.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.2",
    "typescript": "^5.9.3"
  }
}
