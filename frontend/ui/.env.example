# Zuno frontend — env for standalone repo
# Copy to .env and set values. Used at build time (Vite injects VITE_*).
#
# In monorepo: run ./scripts/resolve-env.sh to generate from root .env.
# In frontend-only repo: copy this file to .env and fill in.

# Supabase (auth, realtime)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=

# Backend API URL (required — frontend calls API for all data)
VITE_API_BASE=https://zunoapp.onrender.com

# App URL (for chrome extension; default localhost:5173)
# VITE_APP_URL=http://localhost:5173/

# OAuth deep link scheme (dev APK: com.zuno.app.dev, prod: com.zuno.app)
VITE_APP_SCHEME=com.zuno.app

# Optional: show Feed nav (set to true to enable)
# VITE_SHOW_FEED_DEV=false
# VITE_SHOW_FEED_PROD=false
