# Development only. Copy to .env.development and fill with your **dev** Supabase project.
# Used when running `npm run dev`. Never use production keys here.
#
# Optional: set VITE_API_BASE to your dev Render URL if you want to hit deployed dev backend.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: e.g. https://your-dev-render.onrender.com if using dev Render
# VITE_API_BASE=
