# UI build-time configuration (Vite).
# Copy to .env and set values. Never commit .env with real production keys.
#
# Development: you can leave these empty; the app falls back to dev defaults
# when running with `npm run dev`.
# Production: set all three when building (e.g. in CI or deploy script).

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: API base URL. If UI and API are on same host (e.g. www.zuno.com) leave unset.
# If API is on a different host set it (e.g. https://api.zuno.com). Android emulator uses 10.0.2.2:8000 when unset.
# VITE_API_BASE=
