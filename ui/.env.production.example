# Production build. Copy to .env.production or .env when building for prod.
# Used when running `npm run build`. Set these in CI or Render build env.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Optional: only if API is on a different host (e.g. https://api.zuno.com)
# VITE_API_BASE=
